"use server";

async function createTaskAction() {
  // Verify user authentication
  // Verify if user is member of the project
  // Verify dto validity
  // Verify if project exists
  // Verify if title is unique within the project
  // Verify if statusId belongs to the project
  // Verify if priorityId belongs validity
  // Verify if assignedToId belongs to project members
  // Create the task in the database
}
